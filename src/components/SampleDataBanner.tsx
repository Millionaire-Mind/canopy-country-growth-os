export function SampleDataBanner() {
  return (
    <div className="border-b border-amber-300 bg-amber-50 px-6 py-2 text-center text-xs font-medium text-amber-800">
      SAMPLE DATA — seeded for demonstration. Import real leads via the Lead Command
      Center, or connect integrations on the Data Health screen, to replace it.
    </div>
  );
}
